include: package:flutter_lints/flutter.yaml

linter:
  rules:
    - always_use_package_imports
    - require_trailing_commas

analyzer:
  plugins:
    - custom_lint
  errors:
    # ignore warnings from using json_annotation with freezed
    invalid_annotation_target: ignore