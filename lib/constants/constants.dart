import 'package:flutter/material.dart';

class Constants {
  static const allowedToReviewGames = true;

  static const defaultOutlineInputBorder = OutlineInputBorder(
    borderRadius: BorderRadius.all(
      Radius.circular(12.0),
    ),
  );
}
